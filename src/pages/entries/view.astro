---
import { ReadEntryPage } from '~/components/AppPages'
/*
 * Read-view shell. Firebase Hosting rewrites /entries/{id} to /entries/view.
 * Cream page on dusk frame, no shell heading, no sidebar.
 */
import BaseLayout from '~/layouts/BaseLayout.astro'
---
<BaseLayout title="entry — oriz-journal" noindex={true} hideHeader={true} hideFooter={true}>
  <ReadEntryPage client:only="react" />
</BaseLayout>
